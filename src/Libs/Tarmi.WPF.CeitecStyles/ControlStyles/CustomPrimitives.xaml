<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Tarmi.WPF.CeitecStyles.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../StyleResources.xaml"/>
        <ResourceDictionary Source="../Icons.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type controls:Switch}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundLightBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <ControlTemplate.Resources>
                        <Storyboard x:Key="OnChecking">
                            <ThicknessAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="slider" Storyboard.TargetProperty="Margin">
                                <SplineThicknessKeyFrame KeyTime="00:00:00.1000000" Value="24,0,0,0" />
                            </ThicknessAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="OnUnchecking">
                            <ThicknessAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="slider" Storyboard.TargetProperty="Margin">
                                <SplineThicknessKeyFrame KeyTime="00:00:00.1000000" Value="-24,0,0,0" />
                            </ThicknessAnimationUsingKeyFrames>
                        </Storyboard>
                    </ControlTemplate.Resources>
                    <DockPanel x:Name="dockPanel">
                        <ContentPresenter Margin="0,2,4,2" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Content="{TemplateBinding Content}" ContentStringFormat="{TemplateBinding ContentStringFormat}" ContentTemplate="{TemplateBinding ContentTemplate}" RecognizesAccessKey="True" VerticalAlignment="Center" />
                        <Grid Margin="0,-3,4,0">
                            <Border x:Name="BackgroundBorder" BorderBrush="{StaticResource ForegroundDarkBrush}" Background="{StaticResource BackgroundBrush}" BorderThickness="1" CornerRadius="11" Height="24" Width="50">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                </Grid>
                            </Border>
                            <Border Background="{StaticResource ForegroundBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="slider" Width="18" Height="18" CornerRadius="100" Margin="-24,0,0,0" />

                        </Grid>
                    </DockPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource OnUnchecking}" x:Name="OnUnchecking_BeginStoryboard" />
                            </Trigger.ExitActions>
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource OnChecking}" x:Name="OnChecking_BeginStoryboard" />
                            </Trigger.EnterActions>
                            <Setter TargetName="slider" Property="Background" Value="{StaticResource ForegroundDarkBrush}" />
                            <Setter TargetName="BackgroundBorder" Property="Background" Value="{StaticResource HighlightBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="BackgroundBorder" Property="Background" Value="{StaticResource BackgroundDarkBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BackgroundBorder" Property="BorderBrush" Value="{StaticResource HighlightBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate TargetType="{x:Type controls:ContentSelector}" x:Key="contentSelectorTemplate">
        <ContentPresenter Content="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=controls:ContentSelector}, Mode=OneWay}" />
    </ControlTemplate>

    <Style TargetType="{x:Type controls:ContentSelector}">
        <Setter Property="Template" Value="{StaticResource contentSelectorTemplate}"/>
    </Style>

    <Style TargetType="{x:Type controls:ChannelIndicator}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ChannelIndicator}">
                    <Border Background="{TemplateBinding Background}" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}"
                            MinWidth="{TemplateBinding Diameter}" MinHeight="{TemplateBinding Diameter}">
                        <Ellipse Width="{TemplateBinding Diameter}" Height="{TemplateBinding Diameter}" Fill="{TemplateBinding Color}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type controls:HistogramPolygon}">
        <Setter Property="Stretch" Value="Fill"/>
        <Setter Property="Fill" Value="{StaticResource ForegroundBrush}"/>
    </Style>


    <Style TargetType="{x:Type controls:EditableTextBlock}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:EditableTextBlock}">
                    <TextBlock Text="{Binding Text, RelativeSource= {RelativeSource AncestorType=controls:EditableTextBlock}}"
                               FontSize="{TemplateBinding FontSize}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsInEditMode" Value="True">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type controls:EditableTextBlock}">
                            <ControlTemplate.Resources>
                                <Style TargetType="{x:Type TextBox}">
                                    <Setter Property="Width" Value="{Binding ActualWidth, Mode=OneWay, RelativeSource= {RelativeSource AncestorType=controls:EditableTextBlock}}"/>
                                    <Setter Property="Foreground" Value="{StaticResource ForegroundLightBrush}"/>
                                    <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="KeyboardNavigation.TabNavigation" Value="None"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                    <Setter Property="AllowDrop" Value="true"/>
                                    <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
                                    <Setter Property="SelectionBrush" Value="{StaticResource HighlightBrush}"/>
                                    <Setter Property="FontSize" Value="{Binding FontSize, Mode=OneWay, RelativeSource= {RelativeSource AncestorType=controls:EditableTextBlock}}"/>
                                    <Setter Property="FontWeight" Value="Normal"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type TextBox}">
                                                <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="0" BorderThickness="0,0,0,0" BorderBrush="{StaticResource BackgroundLightBrush}" SnapsToDevicePixels="True">
                                                    <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Margin="-2,0,0,0" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsEnabled" Value="false">
                                                        <Setter Property="Foreground" Value="{StaticResource ForegroundDarkBrush}"/>
                                                        <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="true">
                                                        <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource HighlightBrush }"/>
                                                    </Trigger>
                                                    <Trigger Property="IsKeyboardFocused" Value="true">
                                                        <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource HighlightBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ControlTemplate.Resources>
                            <TextBox x:Name="PART_TextBox" Text="{Binding Text, RelativeSource= {RelativeSource AncestorType=controls:EditableTextBlock}, UpdateSourceTrigger=Explicit}" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type controls:ThreeDotsMenu}" BasedOn="{StaticResource {x:Type ToggleButton}}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ForegroundLightBrush}" />
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="BorderThickness" Value="{StaticResource BorderThickness}" />
        <Setter Property="BorderBrush" Value="{StaticResource BackgroundLightBrush}"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Width" Value="28"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="true" CornerRadius="{StaticResource CornerRadiusThickness}">
                        <Image x:Name="icon" Source="{StaticResource icon_3dots_vertical_white}" Width="16" Height="16" Margin="-1,0,0,0"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource HighlightBrush}"/>
                            <Setter Property="Source" TargetName="icon" Value="{StaticResource icon_3dots_vertical_hover}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" TargetName="border" Value="{StaticResource ForegroundDarkBrush}"/>
                            <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource HighlightBrush}"/>
                            <Setter Property="Source" TargetName="icon" Value="{StaticResource icon_3dots_vertical_hover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Background" TargetName="border" Value="{StaticResource HighlightMutedBrush}"/>
                            <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource HighlightBrush}"/>
                            <Setter Property="Source" TargetName="icon" Value="{StaticResource icon_3dots_vertical_hover}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" TargetName="border" Value="{StaticResource BackgroundDarkBrush}"/>
                            <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource BackgroundLightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type controls:BottomExpander}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:BottomExpander}">
                    <Border>
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel x:Name="PART_Stack">
                                <Border x:Name="PART_MainContent" DockPanel.Dock="Top" Visibility="Collapsed">
                                    <ContentPresenter x:Name="PART_ContentPresenter" Content="{Binding Content, RelativeSource={RelativeSource AncestorType={x:Type controls:BottomExpander}}}" />
                                </Border>
                                <ContentPresenter x:Name="PART_SecondaryContentPresenter" Content="{Binding SecondaryContent, RelativeSource={RelativeSource AncestorType={x:Type controls:BottomExpander}}}"
                                              DockPanel.Dock="Bottom"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="SecondaryContent" Value="{x:Null}">
                            <Setter Property="Visibility" TargetName="PART_MainContent" Value="Visible"/>
                            <Setter Property="Visibility" TargetName="PART_SecondaryContentPresenter" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type controls:ScrollSlider}" BasedOn="{StaticResource {x:Type Slider}}" />
</ResourceDictionary>
