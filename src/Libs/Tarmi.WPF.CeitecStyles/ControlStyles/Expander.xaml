<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../StyleResources.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="ExpanderDownHeaderStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border VerticalAlignment="top" HorizontalAlignment="Left" Margin="10,-20,0,0" x:Name="border">
                        <Grid Background="Transparent" SnapsToDevicePixels="False">
                            <Ellipse x:Name="circle" Fill="{StaticResource BackgroundLightBrush}" HorizontalAlignment="Center" Height="40" Stroke="{StaticResource HighlightBrush}" VerticalAlignment="Center" Width="40"/>
                            <Path x:Name="arrow" Data="{StaticResource downArrow}" HorizontalAlignment="Center" Stroke="{StaticResource HighlightBrush }" SnapsToDevicePixels="false" StrokeThickness="2" VerticalAlignment="Center"/>
                            <ContentPresenter Grid.Column="1" HorizontalAlignment="Left" Margin="4,0,0,0" RecognizesAccessKey="True" SnapsToDevicePixels="True" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="true">
                            <Setter Property="Data" TargetName="arrow" Value="{StaticResource upArrow}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Margin" TargetName="border" Value="20,20,-20,-20"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Fill" TargetName="circle" Value="{StaticResource HighlightMutedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Stroke" TargetName="arrow" Value="{StaticResource ForegroundDarkBrush}"/>
                            <Setter Property="Fill" TargetName="circle" Value="{StaticResource HighlightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ExpanderUpHeaderStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="10,0,0,-20" x:Name="border">
                        <Grid Background="Transparent" SnapsToDevicePixels="False">
                            <Ellipse x:Name="circle" Fill="{StaticResource BackgroundLightBrush}" HorizontalAlignment="Center" Height="40" Stroke="{StaticResource HighlightBrush}" VerticalAlignment="Center" Width="40"/>
                            <Path x:Name="arrow" Data="{StaticResource upArrow}" HorizontalAlignment="Center" Stroke="{StaticResource HighlightBrush}" SnapsToDevicePixels="false" StrokeThickness="2" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="true">
                            <Setter Property="Data" TargetName="arrow" Value="{StaticResource downArrow}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Margin" TargetName="border" Value="20,-20,-20,20"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Fill" TargetName="circle" Value="{StaticResource HighlightMutedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Stroke" TargetName="arrow" Value="{StaticResource ForegroundDarkBrush}"/>
                            <Setter Property="Fill" TargetName="circle" Value="{StaticResource HighlightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ExpanderLeftHeaderStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border VerticalAlignment="Top" HorizontalAlignment="Right" x:Name="border">
                        <Grid Background="Transparent" SnapsToDevicePixels="False" VerticalAlignment="Top">
                            <Ellipse x:Name="circle" Fill="{StaticResource BackgroundLightBrush}" HorizontalAlignment="Center" Height="32" Stroke="{StaticResource HighlightBrush}" VerticalAlignment="Center" Width="32" />
                            <Path x:Name="arrow" Data="{StaticResource leftArrow}" HorizontalAlignment="Center" Stroke="{StaticResource HighlightBrush}" SnapsToDevicePixels="false" StrokeThickness="3" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Data" TargetName="arrow" Value="{StaticResource rightArrow}" />
                            <Setter Property="Margin" TargetName="arrow" Value="2, 0, 0, 0" />
                            <Setter Property="Margin" TargetName="border" Value="0, 8, -16, 0" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Data" TargetName="arrow" Value="{StaticResource leftArrow}" />
                            <Setter Property="Margin" TargetName="arrow" Value="-2, 0, 0, 0" />
                            <Setter Property="Margin" TargetName="border" Value="0, 16, 16, 8"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Fill" TargetName="circle" Value="{StaticResource HighlightMutedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Stroke" TargetName="arrow" Value="{StaticResource ForegroundDarkBrush}"/>
                            <Setter Property="Fill" TargetName="circle" Value="{StaticResource HighlightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ExpanderRightHeaderStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border VerticalAlignment="Top" HorizontalAlignment="Left" x:Name="border">
                        <Grid Background="Transparent" SnapsToDevicePixels="False" VerticalAlignment="Top">
                            <Ellipse x:Name="circle" Fill="{StaticResource BackgroundLightBrush}" HorizontalAlignment="Center" Height="32" Stroke="{StaticResource HighlightBrush}" VerticalAlignment="Center" Width="32" />
                            <Path x:Name="arrow" Data="{StaticResource rightArrow}" HorizontalAlignment="Center" Stroke="{StaticResource HighlightBrush}" SnapsToDevicePixels="false" StrokeThickness="3" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Data" TargetName="arrow" Value="{StaticResource leftArrow}" />
                            <Setter Property="Margin" TargetName="arrow" Value="-2, 0, 0, 0" />
                            <Setter Property="Margin" TargetName="border" Value="-16, 8, 0, 0" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Data" TargetName="arrow" Value="{StaticResource rightArrow}" />
                            <Setter Property="Margin" TargetName="arrow" Value="2, 0, 0, 0" />
                            <Setter Property="Margin" TargetName="border" Value="16, 16, 0, 8"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Fill" TargetName="circle" Value="{StaticResource HighlightMutedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Stroke" TargetName="arrow" Value="{StaticResource ForegroundDarkBrush}"/>
                            <Setter Property="Fill" TargetName="circle" Value="{StaticResource HighlightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style TargetType="{x:Type Expander}" x:Key="headerLessExpander">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Expander}">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                        <DockPanel>
                            <Border DockPanel.Dock="Top" x:Name="ExpandSite" BorderBrush="{StaticResource BackgroundLightBrush}" BorderThickness="0,0,0,1">
                                <ContentPresenter x:Name="Content" Focusable="false" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="0,0,0,25" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Visibility="Collapsed"/>
                            </Border>

                            <ToggleButton x:Name="HeaderSite" DockPanel.Dock="Bottom"
                                          IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" MinWidth="0" MinHeight="0" 
                                          Style="{StaticResource ExpanderDownHeaderStyle}"/>
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="true">
                            <Setter Property="Visibility" TargetName="Content" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter Property="BorderBrush" TargetName="ExpandSite" Value="Transparent"/>
                            <Setter Property="Margin" Value="-10"/>
                        </Trigger>
                        <Trigger Property="ExpandDirection" Value="Right">
                            <Setter Property="DockPanel.Dock" TargetName="ExpandSite" Value="Left"/>
                            <Setter Property="BorderThickness" TargetName="ExpandSite" Value="0,0,1,0"/>
                            <Setter Property="Margin" TargetName="Content" Value="0,0,25,0"/>
                            <Setter Property="DockPanel.Dock" TargetName="HeaderSite" Value="Right"/>
                            <Setter Property="Style" TargetName="HeaderSite" Value="{StaticResource ExpanderRightHeaderStyle}"/>
                        </Trigger>
                        <Trigger Property="ExpandDirection" Value="Up">
                            <Setter Property="DockPanel.Dock" TargetName="ExpandSite" Value="Bottom"/>
                            <Setter Property="BorderThickness" TargetName="ExpandSite" Value="0,1,0,0"/>
                            <Setter Property="Margin" TargetName="Content" Value="0,25,0,0"/>
                            <Setter Property="DockPanel.Dock" TargetName="HeaderSite" Value="Top"/>
                            <Setter Property="Style" TargetName="HeaderSite" Value="{StaticResource ExpanderUpHeaderStyle}"/>
                        </Trigger>
                        <Trigger Property="ExpandDirection" Value="Left">
                            <Setter Property="DockPanel.Dock" TargetName="ExpandSite" Value="Right"/>
                            <Setter Property="BorderThickness" TargetName="ExpandSite" Value="1,0,0,0"/>
                            <Setter Property="Margin" TargetName="Content" Value="25,0,0,0"/>
                            <Setter Property="DockPanel.Dock" TargetName="HeaderSite" Value="Left"/>
                            <Setter Property="Style" TargetName="HeaderSite" Value="{StaticResource ExpanderLeftHeaderStyle}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" Value="0.7"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate TargetType="{x:Type ToggleButton}" x:Key="PlainExpanderDownHeaderTemplate">
        <Border Background="Transparent">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid
                x:Name="ChevronGrid"
                Grid.Column="0"
                Margin="0"
                VerticalAlignment="Center"
                Background="Transparent"
                RenderTransformOrigin="0.5, 0.5">
                    <Grid.RenderTransform>
                        <RotateTransform Angle="0" />
                    </Grid.RenderTransform>
                    <Path x:Name="arrow" Data="{StaticResource rightArrow}" Stroke="White" SnapsToDevicePixels="false" StrokeThickness="3" Width="16" Height="16" />
                </Grid>

                <ContentPresenter
                x:Name="ContentPresenter"
                Grid.Column="1"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Content="{TemplateBinding Content}"
                TextElement.FontSize="{TemplateBinding FontSize}"
                Margin="4,0,0,0"/>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="ChevronGrid"
                                Storyboard.TargetProperty="(Grid.RenderTransform).(RotateTransform.Angle)"
                                To="90"
                                Duration="00:00:00.167" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="ChevronGrid"
                                Storyboard.TargetProperty="(Grid.RenderTransform).(RotateTransform.Angle)"
                                To="0"
                                Duration="00:00:00.167" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>


    <Style TargetType="{x:Type Expander}">
        <Setter Property="Background" Value="{StaticResource HighlightMutedBrush}" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="IsExpanded" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Expander}">
                    <Border BorderBrush="{StaticResource HighlightBrush}" BorderThickness="1" CornerRadius="{StaticResource CornerRadiusThickness}">
                        <DockPanel>
                            <Border x:Name="ToggleButtonBorder"
                                DockPanel.Dock="Top"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource HighlightBrush}" 
                                BorderThickness="0"
                                CornerRadius="{TemplateBinding Border.CornerRadius}">
                                <ToggleButton
                                        x:Name="HeaderSite"
                                        Margin="0"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Center"
                                        HorizontalContentAlignment="Stretch"
                                        VerticalContentAlignment="Center"
                                        Content="{TemplateBinding Header}"
                                        ContentTemplate="{TemplateBinding HeaderTemplate}"
                                        ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}"
                                        Foreground="{TemplateBinding Foreground}"
                                        IsChecked="{Binding Path=IsExpanded,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                        IsEnabled="{TemplateBinding IsEnabled}"
                                        OverridesDefaultStyle="True"
                                        Template="{StaticResource PlainExpanderDownHeaderTemplate}"
                                        Width="NaN" Height="NaN"/>
                            </Border>

                            <Grid x:Name="ContentPresenterGrid" DockPanel.Dock="Bottom" ClipToBounds="True">
                                <Border x:Name="ContentPresenterBorder"
                                    Background="{DynamicResource ExpanderContentBackground}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1,0,1,1"
                                    CornerRadius="0,0,4,4"
                                    Visibility="Collapsed">
                                    <ContentPresenter x:Name="ContentPresenter"
                                                  Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Content="{TemplateBinding Content}" />
                                </Border>
                            </Grid>
                        </DockPanel>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="true">
                            <Setter Property="Visibility" TargetName="ContentPresenterBorder" Value="Visible"/>
                            <Setter Property="BorderThickness" TargetName="ToggleButtonBorder" Value="0,0,0,1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
