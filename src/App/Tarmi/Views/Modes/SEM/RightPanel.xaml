<appControls:ApplicationModeControlBase
    x:Class="Tarmi.App.Views.Modes.SEM.RightPanel"
    x:TypeArguments="viewModels:ElectronBeamModeViewModel"
    xmlns:appControls="clr-namespace:Tarmi.App.WPF.Controls;assembly=Tarmi.App.WPF"
    xmlns:bwpf="clr-namespace:Tarmi.WPF;assembly=Tarmi.WPF"
    xmlns:viewModels="clr-namespace:Tarmi.App.ViewModels.Modes.SEM"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Tarmi.App.Views.Modes.SEM"
    xmlns:modes="clr-namespace:Tarmi.App.Views.Modes"
    xmlns:controls="clr-namespace:Tarmi.WPF.CeitecStyles.Controls;assembly=Tarmi.WPF.CeitecStyles"
    xmlns:betrianTypes="clr-namespace:Tarmi.Devices.Thermofisher.Instrument.Types;assembly=Tarmi.Devices.Thermofisher.Instrument"
    xmlns:views="clr-namespace:Tarmi.App.Views"
    xmlns:localControls="clr-namespace:Tarmi.App.Controls"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="500"
    xmlns:converters="clr-namespace:Tarmi.App.Converters"
    xmlns:appMode="clr-namespace:Tarmi.App.WPF;assembly=Tarmi.App.WPF"
    xmlns:sem="clr-namespace:Tarmi.App.ViewModels.Modes.SEM"
    d:DataContext="{d:DesignInstance Type=sem:ElectronBeamModeViewModel}"
    Tag="{x:Static appMode:ApplicationMode.SEM}"
>
    <UserControl.Resources>
        <converters:EnumDisplayAttributeValueConverter x:Key="EnumDisplayAttributeValueConverter"/>
        <converters:EqualityConverter x:Key="EqualityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="580" />
        </Grid.RowDefinitions>
        <TabControl Grid.Row="0">
            <TabItem Header="ROI">
                <views:ROIs ViewModel="{Binding RoiControl}"/>
            </TabItem>
        </TabControl>
        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedLowerRightPanelTab}">
            <TabItem Header="Stage">
                <views:StageTabUserControl DataContext="{Binding}"/>
            </TabItem>
            <TabItem Header="Tileset">
                <modes:TileSetGrabbingUserControl DataContext="{Binding TileSetGrabbing}"/>
            </TabItem>
        </TabControl>
    </Grid>
</appControls:ApplicationModeControlBase>
