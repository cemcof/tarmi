Implementation uses ThermoFisher xT Object Model COM API.
It's bound to specific xT server version, the set of used functionality is quite small, it may work on other models as well.
One may implement the instrument interface via AutoScript or target any other SEM vendor.
